import 'package:flame/extensions.dart';

// Definimos el tamaño de cada "tile" o cuadro en el juego.
// Usaremos 32x32 píxeles.
const double tileSize = 32.0;

// Vector2 es una forma que usa Flame para guardar posiciones (x, y).
final Vector2 tileVectorSize = Vector2.all(tileSize);